import { ImgShadowDirective } from './img-shadow.directive';

describe('ImgShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgShadowDirective();
    expect(directive).toBeTruthy();
  });
});
